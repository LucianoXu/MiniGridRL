# MiniGridRL

A playground for training, evaluating, and visually demonstrating RL agents on [MiniGrid](https://minigrid.farama.org) environments.

# Installation

1. run the command to sync the python venv: `uv sync`

# Usage

- Use `uv run ...` to run the command in the virtual environment.